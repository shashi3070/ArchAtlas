# Overview
Embeddings convert discrete data (text, images, code) into continuous vector representations. Similar items are close in embedding space, enabling semantic search, clustering, classification, and recommendation.

Embedding models are trained to capture meaning: 'king' - 'man' + 'woman' ≈ 'queen' in vector arithmetic.
