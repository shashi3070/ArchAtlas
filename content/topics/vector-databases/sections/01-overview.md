# Overview
Vector databases store high-dimensional vectors (embeddings) and support similarity search. They are essential for RAG, recommendation systems, image search, and semantic deduplication.

Unlike traditional databases that index by exact values, vector databases index by proximity in embedding space, enabling semantic search.
