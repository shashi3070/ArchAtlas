# Overview
Containers package an application with its dependencies into a lightweight, portable unit that runs consistently across environments. Docker is the most common container runtime; Kubernetes is the standard orchestrator for scheduling, scaling, and managing containers at scale.

Kubernetes abstracts infrastructure: you declare desired state (replicas, resources, health checks) and the controller loop reconciles reality to match.
