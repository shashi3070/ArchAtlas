# Overview
Microservices decompose an application into small, focused services that communicate over well-defined APIs. Each service owns its data store, can be written in a different language, and is deployed independently.

The trade-off is distributed-systems complexity: network partitions, data consistency, observability, and operational overhead all increase. The pattern is most valuable for large teams that need independent deployment velocity and service-specific scaling.
